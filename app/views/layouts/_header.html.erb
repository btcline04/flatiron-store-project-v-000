<nav class="nav">
  <ul class="nav-left">
    <p> <%= link_to "Store Home", store_path %> </p>
  </ul>
  <ul class="nav-right">
    <p> <%= link_to "Sign in", new_user_session_path if !current_user %> </p>
    <p> <%= link_to "Sign up", new_user_registration_path if !current_user %> </p>
    <p> <%= "Signed in as #{current_user.email}" if current_user  %> </p>
    <p> <%= link_to "Sign out", destroy_user_session_path, method: :delete if current_user  %> </p>
    <p> <%= link_to "Cart", cart_path(current_cart) if current_cart %></p>
  </ul>
</nav>